# Core web service
fastapi>=0.115
uvicorn[standard]>=0.30
jinja2>=3.1
python-multipart>=0.0.9

# Security: password hashing + encrypted-at-rest secrets
argon2-cffi>=23.1
cryptography>=43.0

# LAN discovery (mDNS)
zeroconf>=0.131

# Content handling (images, conversions)
pillow>=10.4
